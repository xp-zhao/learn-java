spring:
  cloud:
    nacos:
      config:
        server-addr: nacos.service.com:8848
        namespace: ns-develop
        # group: 使用默认DEFAULT_GROUP 有特殊需要才指定
        file-extension: yaml
        ext-config:
          - dataId: common.yaml
            group: common